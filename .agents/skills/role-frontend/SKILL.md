---
name: role-frontend
description: UX, screen states, and API interactions. Use after security gate for pages, components, loading/error states, UI tests, or contract alignment with backend.
---

# Frontend role

Follow `spec-skills-lib/roles/frontend/spec.md`.

Hand off to **backend** when API contract deltas are needed, with `docs/frontend/ui-contracts.md` and `docs/frontend/test-evidence.md`.

Templates: `.cursor/skills/frontend-role/templates.md`

Playbook: `spec-skills-lib/playbooks/stacks/<stack_profile>.yaml` (detect via `spec-skills-lib/tools/detect-stack.sh`).

Cursor subagent: `.cursor/agents/frontend.md` — invoke with "Use the frontend subagent to …"
