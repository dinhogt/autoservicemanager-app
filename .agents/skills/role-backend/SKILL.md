---
name: role-backend
description: Business rules, APIs, and data integrations. Use after security gate for endpoints, auth flows, integrations, or backend tests.
---

# Backend role

Follow `spec-skills-lib/roles/backend/spec.md`.

Hand off to **documentation** with `docs/backend/api-contract.md` and `docs/backend/test-report.md`.

Playbook: `spec-skills-lib/playbooks/stacks/<stack_profile>.yaml` (detect via `spec-skills-lib/tools/detect-stack.sh`).

Cursor subagent: `.cursor/agents/backend.md` — invoke with "Use the backend subagent to …"
