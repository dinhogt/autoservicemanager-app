# Spec-Skills orchestrator

Read `AGENTS.md`, `spec-skills-lib/rules/global-rules.yaml`, the active flow YAML, and the stack playbook before coding.

Follow `spec-skills-lib/roles/<role>/spec.md` for the current stage. Write artifacts under `docs/`.
