# Workflow: parallel implementation

## Prerequisites
- Security gate passed
- For complex-flow: infrastructure stage complete

## Stages (parallel)
- **frontend** — UI + tests
- **backend** — APIs + tests

## Then
Documentation → observability → QA → support-debug per active flow.
