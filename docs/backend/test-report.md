# Test report — repo-app (2026-08-08)

| Comando | Resultado |
|---------|-----------|
| `yarn lint` | PASS |
| `yarn arch:check` | (ver CI) |
| `yarn test` | **88** suites / **380** testes PASS (+ domain-shared 17) |

Escopo: `AppRole.CLIENTE`, `ClienteAuthGuard`, trace/JSON logger, X-Ray bootstrap.
