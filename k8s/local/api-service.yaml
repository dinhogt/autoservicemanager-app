apiVersion: v1
kind: Service
metadata:
  name: autoservice-api
  namespace: autoservice
  labels:
    app: autoservice-api
spec:
  type: NodePort
  selector:
    app: autoservice-api
  ports:
    - port: 80
      targetPort: 3000
      nodePort: 30080
      protocol: TCP
