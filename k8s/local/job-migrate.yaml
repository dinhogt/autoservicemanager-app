apiVersion: batch/v1
kind: Job
metadata:
  name: autoservice-migrate
  namespace: autoservice
spec:
  ttlSecondsAfterFinished: 300
  template:
    spec:
      serviceAccountName: autoservice-migrate
      restartPolicy: OnFailure
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        fsGroup: 10001
      containers:
        - name: migrate
          image: autoservicemanager:local
          imagePullPolicy: IfNotPresent
          command: ["prisma", "migrate", "deploy"]
          envFrom:
            - secretRef:
                name: autoservice-secrets
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          volumeMounts:
            - name: tmp
              mountPath: /tmp
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 250m
              memory: 256Mi
      volumes:
        - name: tmp
          emptyDir: {}
