---
applyTo: "**"
---

# Security guardrails

Never commit secrets. Validate external input. Do not log credentials or full PII.

Update threat model when boundaries change. Run dependency audits from the stack playbook.

See `spec-skills-lib/rules/security-playbook.yaml`.
