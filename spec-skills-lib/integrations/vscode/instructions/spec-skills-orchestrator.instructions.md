---
applyTo: "**"
---

# Spec-Skills orchestrator v0.4

Before coding, read `AGENTS.md`, `spec-skills-lib/rules/global-rules.yaml`, the active flow YAML, and the stack playbook.

When `opc_mode: true`, use `opc-flow` and read `spec-skills-lib/framework/opc-mode.md`.

Follow the current role spec under `spec-skills-lib/roles/<role>/spec.md`.

Write artifacts under `docs/` per handoff. On caveman gate failure, rerun the failing stage in normal mode.

Official OPC stack: `nestjs-next-react`. Cloud: OCI-first.
