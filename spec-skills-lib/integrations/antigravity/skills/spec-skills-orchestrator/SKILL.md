---
name: spec-skills-orchestrator
description: Load spec-skills flow and global rules before multi-step engineering tasks. Use when starting features, refactors, or release work.
---

# Spec-Skills orchestrator

Read `AGENTS.md` and `spec-skills-lib/rules/global-rules.yaml` first.

Determine active flow (`default-flow` vs `complex-flow`) from `flow_config`.

Follow the matching role spec for the current stage. Full orchestrator: `spec-skills-lib/integrations/_shared/orchestrator.md`
