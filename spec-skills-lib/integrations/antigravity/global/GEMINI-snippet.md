# Spec-Skills (global snippet)

Append to `~/.gemini/GEMINI.md` for workspace-agnostic defaults:

- Prefer reading project `AGENTS.md` when present
- Never commit secrets; validate external input
- Use project `spec-skills-lib/` when installed

Install project rules with `spec-skills-lib/tools/install-ide-integration.sh`.
