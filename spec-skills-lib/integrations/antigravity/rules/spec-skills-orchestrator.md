# Spec-Skills orchestrator

Read `AGENTS.md`, `spec-skills-lib/rules/global-rules.yaml`, the active flow YAML, and the stack playbook before coding.

When `opc_mode: true`, use `opc-flow` and read `spec-skills-lib/framework/opc-mode.md`.

**opc-flow v0.4:** `business` → `discovery` → `digital-business` → `saas` → `requirements` → `ux` → `copywriting` → `architecture` → [`ai-engineering`] → `cloud` → `security` → `legal` → `finops` → implementation → `saas-ops` → `support-debug` → `growth`

**default-flow v0.3:** `business` → `digital-business` → `saas` → `requirements` → `copywriting` → `architecture` → `cloud` → `security` → `finops` → implementation

Follow `spec-skills-lib/roles/<role>/spec.md` for the **current stage only**. See `spec-skills-lib/roles/_boundaries.md`.

Write artifacts under `docs/`.
