# Security guardrails

Never commit secrets. Validate external input. No credentials or full PII in logs.

Update `docs/security/threat-model.md` when boundaries change. See `spec-skills-lib/rules/security-playbook.yaml`.
