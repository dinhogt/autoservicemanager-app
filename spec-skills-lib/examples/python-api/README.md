# Exemplo: Python API

- Stack: `python-django-fastapi`
- Fluxo: `default-flow`
- Security stage obrigatório antes de implementação

## Override

Copie `global-rules.override.yaml` para `spec-skills-lib/rules/global-rules.override.yaml`.

## Comandos típicos

```bash
pytest
ruff check .
pip-audit
```
